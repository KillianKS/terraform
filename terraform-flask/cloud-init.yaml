#cloud-config
package_update: true
packages:
  - python3
  - python3-pip

runcmd:
  - pip3 install flask
  - echo "from flask import Flask
app = Flask(__name__)
@app.route('/')
def hello():
    return 'Hello from Terraform!'
if __name__ == '__main__':
    app.run(host='0.0.0.0', port=80)" > /home/azureuser/app.py
  - nohup python3 /home/azureuser/app.py &
